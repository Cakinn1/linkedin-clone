import React from "react";

interface InputProps {
  string: string;
}

/**
 * Reusable Input component for consistent styling and behavior.
 *
 * @component
 * @example
 *
 *
 *
 *
 *
 */

const Input: React.FC<InputProps> = ({ string }) => {
  return <div>sddadasd</div>;
};

export default Input;
