/**
 * Base URL for all API requests.
 * This should be updated with the actual API endpoint in production.
 * @type {string}
 */
export const API_BASE_URL: string = "https://your-api.com/api";

// ... (Similar JSDoc comments for other endpoints)

/**
 * Error message displayed when login credentials are invalid.
 * @type {string}
 */
export const LOGIN_ERROR_MESSAGE: string = "Invalid username or password";

// ... (Similar JSDoc comments for other error messages)

/**
 * Maximum allowed length for a post.
 * @type {number}
 */
export const MAX_POST_LENGTH: number = 280;

// ... (Similar JSDoc comments for other configuration values)



/**
 * Name of the application.
 * @type {string}
 */
export const APP_NAME: string = "My LinkedIn Clone";
